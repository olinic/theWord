import QtQuick 2.0

/*
  A verse is selectable.
  When selected, it can be highlighted, bookmarked, or given a note.

  When multiple verses are highlighted, all selected verses are highlighted.
  When multiple verses are bookmarked, the first verse is only bookmarked.
  When multiple verses are given a note, the note only appears in the last selected verse.


*/


Rectangle {
    width: 100
    height: 62
}
